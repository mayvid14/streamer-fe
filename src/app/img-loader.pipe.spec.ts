import { ImgLoaderPipe } from './img-loader.pipe';

describe('ImgLoaderPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgLoaderPipe();
    expect(pipe).toBeTruthy();
  });
});
