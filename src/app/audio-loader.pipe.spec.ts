import { AudioLoaderPipe } from './audio-loader.pipe';

describe('AudioLoaderPipe', () => {
  it('create an instance', () => {
    const pipe = new AudioLoaderPipe();
    expect(pipe).toBeTruthy();
  });
});
